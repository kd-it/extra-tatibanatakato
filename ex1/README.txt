# 問題1

手持ちのDocker環境で以下のコマンドを実行し、その出力をコピペでex1.txtに書き込め。
(プロンプトは"$"で示す)
配布時のex1.txtは空ファイルにしてある。

```
$ docker run --rm hello-world
```

出力は複数行にわたります、空白含めてex1.txtに書き込んでください。

記入例

```
Hello from Docker!
...(中略)...
 https://docs.docker.com/get-started/
```

コミット後pushを忘れずに行うこと(移行も同じです)
